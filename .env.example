# Supabase credentials (existing)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# No additional API credentials needed for Open Food Facts API
# Open Food Facts is a free and open database that doesn't require authentication
